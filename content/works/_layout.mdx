---
type: work
---

import {
  Label,
  Summary,
  Metadata,
  RequiredStatement,
  References,
} from "@canopy-iiif/app/ui/server";

<Container variant="wide" style={{display: "flex", gap: "2.618rem"}}>
  <div style={{width: "50%", flexShrink: 0}}>
    <Viewer iiifContent={props.manifest.id} />
  </div>
  <div>
    <header>
      <Label manifest={props.manifest} as="h1" />
      <Summary manifest={props.manifest} as="p" className="canopy-lead" />
    </header>
    <div>
      <References />
      <Metadata manifest={props.manifest} />
      <RequiredStatement manifest={props.manifest} />
    </div>
  </div>
</Container>
<Container>
  <h2>Related Items</h2>
  <RelatedItems iiifContent={props.manifest.id} top={3} />
</Container>
<Container>
  <h2>Contextual References</h2>
  <References emptyLabel="This work has not been referenced yet." />
</Container>
